package controller;

public class superAdminController {
	
	
	
	/*create manager code
	
	payroll_id = 500;
	Users.create(email, name, address, phone, password);
	Employees.create(employee_id, email, Constants.RECEPTIONIST);
	Payrolls.create(payroll_id, Constants.MONTHLY_PAYROLL);
	//need to iterate the months for aggrigate bill 
	
	MonthlyPayrolls.create(payroll_id, month_of_start, compensation);
	hasPay.create(payroll_id, employee_id);
	//add check if receptionist is already there for the service centre ???
	
	HasManager.create(Sid, employee_id);
	*/
}
